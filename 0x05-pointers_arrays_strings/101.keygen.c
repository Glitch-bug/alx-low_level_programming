#include <stdio.h>
#include <time.h>

int select(int min, int max);
char* main()
{

        srand(time(NULL));
	printf("fuck?%d", r);
        return "fuck";
}

int select(int min, int max)
{
        int r = 0;
	while (r < min)
	{
		r = rand(2) % max + 1;
	}
}
