#include <stdio.h>
/**
 * main - prints its name followed by a new line
 * @argc: number of arguements entered
 * @argv: array  of arguement pointers
 * Return: zero
 */

int main(int argc, char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
